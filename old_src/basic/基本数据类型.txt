数字
    类型: 
        i8, i16, i32, i64, i128, isize
        u8, u16, u32, u64, u128, usize
        f32, f64

    特殊值
        f32::INFINITY
        F32::NAN
        (f64 同理)

    转换
        值 as 类型 -> 转换后的值
        小数部分会被直接截断,
        整数部分, 按照空间溢出模式进行转换

文本
    类型
        字符(u32):   char
        字符串:      str
    特性
        编译时长度固定, 不能更改
