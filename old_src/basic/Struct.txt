(1) 声明时, 字段为 `key: value_type`, 
    实例化时, 字段为 `key: value`

(2) 分类: 
    [1] 类单元结构体
    [2] 元组结构体
    [3] 字段结构体

(3) 可变性必须针对整体, 不能只让某个字段可变

(4) 字段结构体使用 . 访问子字段

(5) 若实例化前, 有个变量与字段同名,
    则可以使用 '字段初始化简写语法'

(6) 使用 '结构体更新语法', 实现类似属性继承的功能,
    原理是带有 = 的赋值