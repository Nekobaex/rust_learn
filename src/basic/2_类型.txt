分类
    值类 (4)
        !   / never

        ()  / unit

        bool
        char

        i8, i16, i32, i64, i128, isize
        u8, u16, u32, u64, u128, usize
        f32, f64

    引用类 (3)
        [type; len]         / array
        (type1, type2, ...) / tuple
        &[range]            / slice
        str

        fn()    / fn

        &       / ref
        *       / unref

字面量

转换

别名
    type int = i32;